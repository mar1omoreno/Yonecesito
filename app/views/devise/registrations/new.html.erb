<div class="row top-buffer80">
</div>
<div class="row ">
	<div class="col-md-5 col-md-offset-4"><h4 style="color:737171">Encuentra al profesional correcto, es gratis.</h4></div>
</div>
<div class="row top-buffer30">
	<div class="col-md-4 col-md-push-4 grey paddingform">
		<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
		<%= devise_error_messages! %>
		
			<div class="form-group">
				<!--<label for="exampleName">Nombre completo</label>
				<input type="text" class="form-control" id="exampleName" placeholder="">-->
				<%= f.label :Nombre %><br />
				<%= f.text_field :nombre,  autocomplete: "true", :class => 'form-control' %>
    		
			</div>
			<div class="form-group">
				<!--<label for="exampleName">Nombre completo</label>
				<input type="text" class="form-control" id="exampleName" placeholder="">-->
				<%= f.label :Apellido %><br />
				<%= f.text_field :apellido,  autocomplete: "true", :class => 'form-control' %>
    		
			</div>
			<div class="form-group">
				<!--<label for="exampleMail">Correo electrónico</label>
				<input type="email" class="form-control" id="exampleMail" placeholder="">-->
				<%= f.label :Correo_electrónico %><br />
    			<%= f.email_field :email,  autocomplete: "true", :class => 'form-control' %>
			</div>
			<div class="form-group">
				<!--<label for="examplePassword">Contraseña</label>
				<input type="password" class="form-control" id="examplePassword" placeholder="">-->
				    <%= f.label :Contraseña %>
    				<% if @validatable %>    				
    				<% end %><br />
    				<%= f.password_field :password, autocomplete: "off" , :class => 'form-control'%>
			</div>
			<p>Al hacer click sobre el botón Crear cuenta indicas que has leido y estas de acuerdo con las <a href="<%= url_for inicio_politicas_path%>">Políticas de Privacidad</a>, así como los <a href="<%= url_for inicio_terminos_path%>">Términos y Condiciones</a> del sitio.</p>
					<!--<button type="button" class="btn btn-primary creaCuenta">Crear Cuenta</button>-->
					<%= f.submit "Crear Cuenta", :class => 'btn btn-primary creaCuenta', :style =>"font-weight:bold;" %>
		<% end %>
	</div>
</div>

<% if !@esconder_facebook %>
<div >
	<div class="row top-buffer10">
		<div class="col-md-6 col-md-offset-3 text-center">
			<h4>Ó</h4>
		</div>
	</div>
	<div class="row top-buffer10">
		<div class="col-md-4 col-md-offset-4 paddingform">
			<!--<button type="button" class="btn btn-primary creaCuentaFB text-center"><i class="fa fa-facebook fa-2x whitelinks"></i> &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;Entrar con Facebook</button>-->
	    <%= render "devise/shared/links" %>
		</div>
	</div>
</div>
<% end %>
<div class="row top-buffer150 footerbck">
</div>
